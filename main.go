package main

// This file is intentionally left blank to avoid "no main function" error when building the project.
// The actual application entry point is in cmd/server/main.go, which imports the necessary packages and starts the server.
// You can run the server using `go run cmd/server/main.go` or build it with `go build -o saasapp cmd/server/main.go` and then execute `./saasapp`.
func main() {

}
